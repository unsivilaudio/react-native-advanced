import { View, Text, StyleSheet } from 'react-native';

export default function DeckScreen() {
    return (
        <View>
            <Text>The Deck screen</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
