import { View, Text, StyleSheet } from 'react-native';

export default function ReviewScreen() {
    return (
        <View>
            <Text>The Review screen</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
